#!/bin/sh

#this downloads any available updates to make sure we have the most current version
sudo apt update

#installs python, apache, and flask
sudo apt install python3
sudo apt install apache2 -y
sudo pip install flask

#installs a fork of MySQL, called maria data base
sudo apt install mariadb-server -y

#need to find out how to automate inputting passwords
#sudo mysql_secure_installation

